# tools/test — test suite.

set shell := ["bash", "-c"]

# Run the full test suite
test:
    uv run pytest

# Quiet run
quick:
    uv run pytest -q

# With coverage
coverage:
    uv run pytest --cov=src --cov-report=term-missing --cov-report=html
