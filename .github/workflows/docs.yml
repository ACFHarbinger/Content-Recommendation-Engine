name: Docs

on:
  workflow_dispatch:
  push:
    branches: [main, master]
    paths:
      - "docs/**"
      - "docs/website/**"
      - "mkdocs.yml"

permissions:
  contents: write

concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
      - run: uv venv .venv
      - run: uv pip install --python .venv/bin/python mkdocs-material mkdocstrings[python]
      - run: .venv/bin/mkdocs build --config-file docs/mkdocs.yml
      - uses: actions/setup-node@v7
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: docs/website/package-lock.json
      - run: npm ci
        working-directory: docs/website
      - run: npm run build
        working-directory: docs/website
        env:
          SITE_BASE: /${{ github.event.repository.name }}/app/
      - run: mkdir -p build/gen/site && cp -r site/. build/gen/site/
      - run: mkdir -p build/gen/site/app && cp -r docs/website/dist/. build/gen/site/app/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.REPO_PROJECT_TOKEN }}
          publish_dir: ./build/gen/site
